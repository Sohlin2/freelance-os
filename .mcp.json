{
  "mcpServers": {
    "freelance-os": {
      "type": "http",
      "url": "${user_config.FREELANCEOS_SERVER_URL}",
      "headers": {
        "Authorization": "Bearer ${user_config.FREELANCEOS_API_KEY}"
      }
    }
  }
}
