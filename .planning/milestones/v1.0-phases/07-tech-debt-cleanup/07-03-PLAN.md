---
phase: 07-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - .planning/phases/01-data-foundation/01-VALIDATION.md
  - .planning/phases/03-full-tool-suite/03-VALIDATION.md
  - .planning/phases/04-skill-pack/04-VALIDATION.md
  - .planning/phases/05-plugin-packaging/05-VALIDATION.md
  - .planning/phases/06-critical-integration-fixes/06-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every phase (1, 3, 4, 5, 6) has a VALIDATION.md with nyquist_compliant: true or a clear explanation of what remains"
    - "Phase 6 has a VALIDATION.md file (currently missing entirely)"
    - "Each VALIDATION.md documents the actual test commands, coverage, and sampling strategy"
  artifacts:
    - path: ".planning/phases/01-data-foundation/01-VALIDATION.md"
      provides: "Updated Nyquist validation"
      contains: "nyquist_compliant"
    - path: ".planning/phases/03-full-tool-suite/03-VALIDATION.md"
      provides: "Updated Nyquist validation"
      contains: "nyquist_compliant"
    - path: ".planning/phases/04-skill-pack/04-VALIDATION.md"
      provides: "Updated Nyquist validation"
      contains: "nyquist_compliant"
    - path: ".planning/phases/05-plugin-packaging/05-VALIDATION.md"
      provides: "Updated Nyquist validation"
      contains: "nyquist_compliant"
    - path: ".planning/phases/06-critical-integration-fixes/06-VALIDATION.md"
      provides: "New Nyquist validation"
      contains: "nyquist_compliant"
  key_links: []
---

<objective>
Update Nyquist VALIDATION.md files for phases 1, 3, 4, 5 (partial compliance) and create a new one for phase 6 (missing entirely).

Purpose: Close tech debt item 10 from the v1.0 milestone audit. Validation docs must accurately reflect the test infrastructure and sampling strategy for each phase.
Output: 4 updated VALIDATION.md files + 1 new VALIDATION.md for phase 6
</objective>

<execution_context>
@C:/freelance-os/.claude/get-shit-done/workflows/execute-plan.md
@C:/freelance-os/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/02-mcp-server-core/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update partial VALIDATION.md files for phases 1, 3, 4, 5</name>
  <files>
    .planning/phases/01-data-foundation/01-VALIDATION.md
    .planning/phases/03-full-tool-suite/03-VALIDATION.md
    .planning/phases/04-skill-pack/04-VALIDATION.md
    .planning/phases/05-plugin-packaging/05-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-VALIDATION.md
    .planning/phases/03-full-tool-suite/03-VALIDATION.md
    .planning/phases/04-skill-pack/04-VALIDATION.md
    .planning/phases/05-plugin-packaging/05-VALIDATION.md
    .planning/phases/02-mcp-server-core/02-VALIDATION.md
    .planning/phases/01-data-foundation/01-VERIFICATION.md
    .planning/phases/03-full-tool-suite/03-VERIFICATION.md
    .planning/phases/04-skill-pack/04-VERIFICATION.md
    .planning/phases/05-plugin-packaging/05-VERIFICATION.md
  </read_first>
  <action>
Read each VALIDATION.md and its corresponding VERIFICATION.md. Use the Phase 02 VALIDATION.md as the reference for what a Nyquist-compliant file looks like (it is the only compliant one).

For each of the 4 partial VALIDATION.md files, make these updates:

1. Set frontmatter `nyquist_compliant: true` (change from `false`) and `status: complete` (change from `draft`)

2. Set `wave_0_complete: true` if the phase has a vitest config and tests run successfully

3. Ensure "Test Infrastructure" section documents: Framework, Config file path, Quick run command, Full suite command, Estimated runtime

4. Ensure "Sampling Rate" section documents the per-task-commit sampling strategy

5. Ensure "Feedback Signals" section lists what test output to monitor

6. Ensure "Compliance Checklist" section has all items checked: Wave 0 tests exist, Quick run command works, Sampling rate defined, Feedback signals documented

Exact test commands per phase:
- Phase 1: `npx supabase test db` (pgTAP, requires Docker) + `npx vitest run` (TypeScript types)
- Phase 3: `npx vitest run tests/tools/proposals.test.ts tests/tools/invoices.test.ts tests/tools/time-entries.test.ts tests/tools/scope.test.ts tests/tools/follow-ups.test.ts`
- Phase 4: `npx vitest run tests/skills/` (token budget, skill content)
- Phase 5: `npx vitest run tests/plugin-package.test.ts` (manifest, build, pack, secrets)

Do NOT invent test counts or results. Reference the VERIFICATION.md for each phase to get actual test counts (Phase 3: 102 tests, Phase 4: 104 total, Phase 5: 145 total).
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/01-data-foundation/01-VALIDATION.md .planning/phases/03-full-tool-suite/03-VALIDATION.md .planning/phases/04-skill-pack/04-VALIDATION.md .planning/phases/05-plugin-packaging/05-VALIDATION.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep "nyquist_compliant: true" returns matches for all 4 files
    - grep "status: complete" returns matches for all 4 files
    - grep "Quick run command" returns matches for all 4 files (section exists)
    - grep "npx vitest run" returns matches for phases 3, 4, 5 VALIDATION.md files
    - Phase 01 VALIDATION.md contains "npx supabase test db"
  </acceptance_criteria>
  <done>All 4 partial VALIDATION.md files are updated to Nyquist-compliant format with accurate test infrastructure documentation</done>
</task>

<task type="auto">
  <name>Task 2: Create VALIDATION.md for Phase 6</name>
  <files>
    .planning/phases/06-critical-integration-fixes/06-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/02-mcp-server-core/02-VALIDATION.md
    .planning/phases/06-critical-integration-fixes/06-VERIFICATION.md
    .planning/phases/06-critical-integration-fixes/06-01-SUMMARY.md
  </read_first>
  <action>
Create `.planning/phases/06-critical-integration-fixes/06-VALIDATION.md` following the Phase 02 VALIDATION.md format (the Nyquist-compliant reference).

The file must have YAML frontmatter with: phase: 6, slug: critical-integration-fixes, status: complete, nyquist_compliant: true, wave_0_complete: true, created: 2026-03-28.

Title: "# Phase 6 -- Validation Strategy"

Test Infrastructure table with: Framework = vitest 2.x + pgTAP, Config file = vitest.config.ts, Quick run command = `npx vitest run`, Full suite command = `npx vitest run` followed by `npx supabase test db`, Estimated runtime = ~10 seconds (vitest only; pgTAP requires Docker).

Sampling Rate section: After every task commit run `npx vitest run`. Before phase completion run full suite including pgTAP if Docker available.

Phase 6 Test Coverage table with 3 rows:
- RLS session scope | supabase/tests/rls_session_scope.test.sql | set_app_user_id uses session scope
- Runtime dependencies | Verified by npm install + server startup | express and zod resolve at runtime
- Existing tool tests | tests/tools/*.test.ts | No regressions from migration 000009

Feedback Signals section: vitest all existing tests pass, pgTAP rls_session_scope.test.sql passes, npm install no missing module errors.

Compliance Checklist with all items checked: Wave 0 tests exist, Quick run command works, Sampling rate defined, Feedback signals documented.

Use horizontal rules (three dashes) between major sections, matching the Phase 02 reference format.

Key Phase 6 artifacts for reference:
- Migration: supabase/migrations/20260328000009_fix_set_app_user_id_scope.sql
- pgTAP test: supabase/tests/rls_session_scope.test.sql
- Modified: src/lib/with-user-context.ts, package.json
  </action>
  <verify>
    <automated>test -f .planning/phases/06-critical-integration-fixes/06-VALIDATION.md && grep "nyquist_compliant: true" .planning/phases/06-critical-integration-fixes/06-VALIDATION.md && echo "EXISTS_AND_COMPLIANT"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/06-critical-integration-fixes/06-VALIDATION.md
    - grep "nyquist_compliant: true" returns a match
    - grep "status: complete" returns a match
    - grep "rls_session_scope" returns a match (phase-specific test referenced)
    - grep "npx vitest run" returns a match (quick run command documented)
    - File has valid YAML frontmatter (starts with three dashes and has a closing three dashes)
  </acceptance_criteria>
  <done>Phase 6 has a VALIDATION.md file documenting its test infrastructure and Nyquist compliance</done>
</task>

</tasks>

<verification>
- All 5 phase VALIDATION.md files exist and contain nyquist_compliant: true
- Phase 6 VALIDATION.md is newly created (was missing)
- Each file documents test framework, commands, sampling rate, and feedback signals
- No VALIDATION.md files remain with status: draft or nyquist_compliant: false
</verification>

<success_criteria>
- Phases 1, 3, 4, 5 VALIDATION.md updated from partial to compliant
- Phase 6 VALIDATION.md created from scratch
- All 6 phases (including Phase 2 which was already compliant) now have nyquist_compliant: true
- Nyquist overall status can be upgraded from PARTIAL to COMPLIANT
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-03-SUMMARY.md`
</output>
